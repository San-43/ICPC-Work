#include "bits/stdc++.h"


using namespace std;

int main () {
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);

    int n;
    cin >> n;
    vector<int> a(n);
    for(auto &e : a) cin >> e;

    string s;
    cin >> s;
    
    int b = 1;
    long long ans = 0;

    for(int i = 0; i < s.length(); i++) {
        while(s[i] == '=')
    }
}